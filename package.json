{
  "name": "campus-collab-hub",
  "private": true,
  "version": "1.0.0",
  "description": "Campus Collaboration Hub - MERN platform for student project collaboration",
  "scripts": {
    "install:all": "npm install && npm --prefix server install && npm --prefix client install",
    "dev": "concurrently -n server,client -c green,cyan \"npm --prefix server run dev\" \"npm --prefix client run dev\"",
    "build": "npm --prefix client run build",
    "start": "npm --prefix server run start"
  },
  "devDependencies": {
    "concurrently": "^9.2.0"
  }
}

